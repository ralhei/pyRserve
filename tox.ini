[flake8]
max_line_length = 80
per-file-ignores =
    pyRserve/__init__.py: F401
    # Much improved readability if we allow for extra space character in
    # various places for code formatting. So disable some errors for flake8:
    pyRserve/rtypes.py: E114,E116,E221,E222
